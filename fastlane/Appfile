app_identifier("com.sambender.Sweep")
apple_id(ENV["APPLE_ID"] || "sambndr@uw.edu")
team_id("2VJY2AJVXJ")

for_platform :ios do
  for_lane :beta do
    app_identifier("com.sambender.Sweep")
  end

  for_lane :release do
    app_identifier("com.sambender.Sweep")
  end
end
